# AIマーケティング機能 実装計画

**作成日**: 2026-01-31
**対象**: MarketingHub
**根拠**: `evidence.jsonl` (15件)

---

## System Overview（システム概要）

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         MarketingHub AI Layer                           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌────────────┐  │
│  │  感情分析     │  │ コンテンツ生成 │  │ 予測分析     │  │  A/Bテスト  │  │
│  │  Engine      │  │  Engine      │  │  Engine      │  │  Engine   │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └─────┬──────┘  │
│         │                 │                 │                │         │
│         └────────────────┬┴─────────────────┴────────────────┘         │
│                          │                                             │
│                          ▼                                             │
│                   ┌──────────────┐                                     │
│                   │ Claude API   │                                     │
│                   │ (Anthropic)  │                                     │
│                   └──────────────┘                                     │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                         MCP Integration Layer                           │
├─────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                  │
│  │ HubSpot MCP  │  │ Analytics MCP│  │ Sequenzy MCP │                  │
│  │ (CRM連携)    │  │ (分析連携)   │  │ (メール生成) │                  │
│  └──────────────┘  └──────────────┘  └──────────────┘                  │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Components（コンポーネント）

### 1. 感情分析エンジン（sentiment-analyzer.ts）

**目的**: メッセージから感情を分析し、チャーン予兆を検出

**機能**:
- リアルタイム感情分類（positive/neutral/negative）
- 感情スコアリング（0-100）
- ネガティブ感情トレンド検出
- チャーンアラート生成
- エスカレーション自動トリガー

**根拠**: ev-009, ev-010（感情分析で維持率25%向上）

### 2. AIコンテンツ生成エンジン（content-generator.ts）

**目的**: パーソナライズされたメッセージを自動生成

**機能**:
- セグメント別メッセージ生成
- 件名/本文の自動最適化
- トーン調整（フォーマル/カジュアル）
- 多言語対応（日本語/英語）
- バリエーション自動生成（A/Bテスト用）

**根拠**: ev-003, ev-008, ev-013（パーソナライゼーションで10pp成長、開封率38%向上）

### 3. 予測分析エンジン（predictive-engine.ts）

**目的**: コンバージョン/チャーン/LTVを予測

**機能**:
- コンバージョン確率予測
- チャーンリスクスコア
- 予測LTV算出
- 最適アクションレコメンド
- 予測精度トラッキング

**根拠**: ev-004, ev-005（2.9倍収益成長、85-92%精度）

### 4. A/Bテスト最適化エンジン（ab-optimizer.ts）

**目的**: テストの自動最適化と勝者決定

**機能**:
- Multi-Armed Bandit実装
- 動的トラフィック配分
- 統計的有意性判定
- 自動勝者決定
- 結果レポート生成

**根拠**: ev-011（23%コンバージョン増）

### 5. MCP統合レイヤー（mcp-integrations/）

**目的**: 外部ツールとの連携

**サブコンポーネント**:
- `hubspot.ts` - CRM双方向同期
- `analytics.ts` - GA4連携
- `sequenzy.ts` - AIメールシーケンス

**根拠**: ev-001, ev-006, ev-007, ev-014

---

## Data Model（データモデル）

### 新規テーブル

```prisma
// 感情分析結果
model SentimentAnalysis {
  id          String   @id @default(cuid())
  contactId   String
  messageId   String?

  sentiment   String   // positive, neutral, negative
  score       Float    // 0-100
  emotions    Json     // ["興味", "不安", "期待"]
  confidence  Float    // 0-1

  // チャーンリスク
  churnSignal Boolean  @default(false)
  escalated   Boolean  @default(false)

  analyzedAt  DateTime @default(now())

  @@index([contactId])
  @@index([sentiment])
  @@index([churnSignal])
}

// AIコンテンツ生成履歴
model GeneratedContent {
  id          String   @id @default(cuid())
  tenantId    String

  type        String   // email_subject, email_body, line_message
  input       Json     // 入力パラメータ
  output      String   // 生成されたコンテンツ

  // パフォーマンス追跡
  usedCount   Int      @default(0)
  openRate    Float?
  clickRate   Float?

  generatedAt DateTime @default(now())

  @@index([tenantId])
  @@index([type])
}

// A/Bテスト
model ABTest {
  id          String   @id @default(cuid())
  tenantId    String

  name        String
  status      String   @default("draft") // draft, running, completed

  // バリアント
  variants    Json     // [{id, name, content, weight}]

  // 結果
  results     Json?    // 各バリアントの成績
  winnerId    String?

  // Multi-Armed Bandit設定
  algorithm   String   @default("epsilon_greedy")
  epsilon     Float    @default(0.1)

  startedAt   DateTime?
  endedAt     DateTime?
  createdAt   DateTime @default(now())

  @@index([tenantId])
  @@index([status])
}

// 予測スコア履歴
model PredictionLog {
  id          String   @id @default(cuid())
  contactId   String

  type        String   // conversion, churn, ltv
  score       Float
  confidence  Float
  factors     Json     // スコアに影響した要因

  // 実績との比較（後から更新）
  actualOutcome Boolean?

  predictedAt DateTime @default(now())

  @@index([contactId])
  @@index([type])
}
```

---

## Task Breakdown（タスク分解）

### Phase 1: MVP（2週間）

| # | タスク | 優先度 | 工数 |
|---|--------|--------|------|
| 1.1 | 感情分析エンジン実装 | High | 3日 |
| 1.2 | 感情分析UIダッシュボード | High | 2日 |
| 1.3 | AIコンテンツ生成API | High | 3日 |
| 1.4 | コンテンツ生成UI | High | 2日 |
| 1.5 | Prismaスキーマ更新 | High | 1日 |
| 1.6 | 基本テスト | High | 2日 |

### Phase 2: 拡張（2週間）

| # | タスク | 優先度 | 工数 |
|---|--------|--------|------|
| 2.1 | 予測分析エンジン実装 | Medium | 4日 |
| 2.2 | A/Bテストエンジン実装 | Medium | 3日 |
| 2.3 | チャーンアラート実装 | Medium | 2日 |
| 2.4 | 予測ダッシュボードUI | Medium | 3日 |
| 2.5 | 統合テスト | Medium | 2日 |

### Phase 3: MCP統合（1週間）

| # | タスク | 優先度 | 工数 |
|---|--------|--------|------|
| 3.1 | HubSpot MCP設定 | Low | 2日 |
| 3.2 | 同期ロジック実装 | Low | 2日 |
| 3.3 | エラーハンドリング | Low | 1日 |

---

## Test Plan（テスト計画）

### ユニットテスト

```typescript
// sentiment-analyzer.test.ts
describe('SentimentAnalyzer', () => {
  test('ポジティブメッセージを正しく分類', async () => {
    const result = await analyzeSentiment('この商品最高です！');
    expect(result.sentiment).toBe('positive');
    expect(result.score).toBeGreaterThan(70);
  });

  test('ネガティブメッセージでチャーンシグナル検出', async () => {
    const result = await analyzeSentiment('もう解約したい');
    expect(result.sentiment).toBe('negative');
    expect(result.churnSignal).toBe(true);
  });
});
```

### 統合テスト

- エンドツーエンドのメッセージフロー
- MCP接続テスト
- パフォーマンステスト（100メッセージ/秒）

### データ品質テスト

- 日本語感情分析精度 > 80%
- 予測精度追跡と閾値チェック

---

## Ops（運用設計）

### 環境変数

```env
# AI
ANTHROPIC_API_KEY=sk-ant-...
AI_MODEL=claude-sonnet-4-20250514
AI_MAX_TOKENS=1000

# MCP
HUBSPOT_MCP_URL=https://...
HUBSPOT_ACCESS_TOKEN=...

# Feature Flags
ENABLE_SENTIMENT_ANALYSIS=true
ENABLE_CONTENT_GENERATION=true
ENABLE_PREDICTIVE_ANALYTICS=false
```

### モニタリング

- AI API使用量/コスト追跡
- 予測精度ダッシュボード
- エラー率アラート（> 5%で通知）

### セキュリティ

- APIキーはSecrets Manager経由
- 顧客データはログに含めない
- 感情分析結果は匿名化オプション

---

## 実装開始ファイル

```
src/lib/ai/
├── claude.ts              ✅ 既存
├── trigger-engine.ts      ✅ 既存
├── scoring.ts             ✅ 既存
├── branching.ts           ✅ 既存
├── intent-analyzer.ts     ✅ 既存
├── sentiment-analyzer.ts  🆕 Phase 1
├── content-generator.ts   🆕 Phase 1
├── predictive-engine.ts   🆕 Phase 2
└── ab-optimizer.ts        🆕 Phase 2

src/lib/mcp/
├── hubspot.ts             🆕 Phase 3
├── analytics.ts           🆕 Phase 3
└── sequenzy.ts            🆕 Phase 3

src/app/(dashboard)/
├── automation/
│   ├── page.tsx           ✅ 既存
│   ├── intent/page.tsx    ✅ 既存
│   ├── sentiment/page.tsx 🆕 Phase 1
│   └── ab-tests/page.tsx  🆕 Phase 2
└── ai/
    ├── content/page.tsx   🆕 Phase 1
    └── predictions/page.tsx 🆕 Phase 2
```

---

## 次のアクション

1. **今すぐ**: `sentiment-analyzer.ts` の実装開始
2. **今日中**: Prismaスキーマに新モデル追加
3. **明日**: 感情分析ダッシュボードUI作成
